# HCU (HYGON) backend specific environment variables
# FLAGGEMS_BACKEND corresponds to CMakeLists.txt: set(FLAGGEMS_BACKEND "HCU" ...)
#FLAGGEMS_BACKEND=HCU
#CMAKE_ARGS="-DFLAGGEMS_BUILD_C_EXTENSIONS=ON -DCMAKE_BUILD_TYPE=Release -DFLAGGEMS_BACKEND=HCU"
