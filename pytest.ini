[pytest]
testpaths = tests
pythonpath = src
filterwarnings =
    ignore::pytest.PytestUnknownMarkWarning
    ignore::DeprecationWarning
    ignore:Warning only once for all operators,  other operators may also be .*:UserWarning
markers =
    svd: marks SVD operator tests and benchmarks
