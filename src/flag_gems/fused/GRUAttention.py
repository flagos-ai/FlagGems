# Copyright 2026, The FlagOS Contributors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# Copyright 2026, Yuanzhu Li
# Copyright 2026, BAAI (Beijing Academy of Artificial Intelligence)
# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
import logging

import torch
import triton
import triton.language as tl

from flag_gems.utils import libentry

logger = logging.getLogger(__name__)


@libentry()
@triton.jit
def GRUAttention_kernel(
    query,
    key,
    value,
    output_tensor,
    scale: tl.constexpr,
    B: tl.constexpr,
    H: tl.constexpr,
    N: tl.constexpr,
    D: tl.constexpr,
):
    """
    GRU-style Attention kernel.

    This implements a scaled dot-product attention mechanism:
    - Computes attention scores between query and key
    - Applies softmax to get attention weights
    - Returns weighted sum of values

    Args:
        query: (B, H, N, D) - Query tensor
        key: (B, H, N, D) - Key tensor
        value: (B, H, N, D) - Value tensor
        output_tensor: (B, H, N, D) - Output tensor
    """
    # Get program IDs
    batch_idx = tl.program_id(0)
    head_idx = tl.program_id(1)
    seq_idx = tl.program_id(2)

    # Compute offsets
    query_offset = batch_idx * H * N * D + head_idx * N * D + seq_idx * D
    key_offset = batch_idx * H * N * D + head_idx * N * D
    value_offset = batch_idx * H * N * D + head_idx * N * D
    output_offset = batch_idx * H * N * D + head_idx * N * D + seq_idx * D

    offs_d = tl.arange(0, D)
    offs_n = tl.arange(0, N)

    # Load query - shape (D,)
    q_ptrs = query + query_offset + offs_d
    q = tl.load(q_ptrs).to(tl.float32)

    # Load all keys - shape (N, D)
    k_ptrs = key + key_offset + offs_n[:, None] * D + offs_d[None, :]
    k = tl.load(k_ptrs).to(tl.float32)

    # Compute qk scores: (D,) @ (D, N) -> (N,)
    qk = tl.sum(q[None, :] * k, axis=1) * scale

    # Softmax
    qk_max = tl.max(qk, axis=0)
    qk_shifted = qk - qk_max
    exp_qk = tl.exp(qk_shifted)
    attn_weights = exp_qk / (tl.sum(exp_qk, axis=0) + 1e-10)

    # Load all values - shape (N, D)
    v_ptrs = value + value_offset + offs_n[:, None] * D + offs_d[None, :]
    v = tl.load(v_ptrs).to(tl.float32)

    # Compute weighted sum: (N,) @ (N, D) -> (D,)
    output_val = tl.sum(attn_weights[:, None] * v, axis=0)

    # Store output - convert to the output tensor's dtype
    # output_tensor.dtype gives us the element type
    output_elem_ty = output_tensor.dtype.element_ty
    o_ptrs = output_tensor + output_offset + offs_d
    tl.store(o_ptrs, output_val.to(output_elem_ty))


def GRUAttention(query, key, value, scale=None):
    """
    GRU-style Attention function.

    This implements a scaled dot-product attention mechanism:
    - Computes attention scores between query and key
    - Applies softmax to get attention weights
    - Returns weighted sum of values

    Args:
        query: Query tensor of shape (B, H, N, D)
        key: Key tensor of shape (B, H, N, D)
        value: Value tensor of shape (B, H, N, D)
        scale: Optional scale factor for attention scores

    Returns:
        Output tensor of shape (B, H, N, D)
    """
    logger.debug("GEMS GRU_ATTENTION")

    assert query.dtype in (
        torch.float32,
        torch.float16,
    ), f"Only float32/float16 supported, got {query.dtype}"
    B, H, N, D = query.shape

    if scale is None:
        scale = 1.0 / (D**0.5)

    output = torch.empty_like(query)

    # Define grid
    grid = (B, H, N)

    GRUAttention_kernel[grid](
        query,
        key,
        value,
        output,
        scale,
        B,
        H,
        N,
        D,
    )

    return output
