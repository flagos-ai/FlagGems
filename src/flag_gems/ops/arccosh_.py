# Copyright 2026, The FlagOS Contributors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
import logging

import torch
import triton
import triton.language as tl

from flag_gems.runtime import torch_device_fn

logger = logging.getLogger(__name__)


@triton.jit
def arccosh_kernel_(x_ptr, n_elements, BLOCK_SIZE: tl.constexpr):
    pid = tl.program_id(axis=0)
    block_start = pid * BLOCK_SIZE
    offsets = block_start + tl.arange(0, BLOCK_SIZE)
    mask = offsets < n_elements

    x = tl.load(x_ptr + offsets, mask=mask)
    x32 = x.to(tl.float32)

    # acosh(x) = log(x + sqrt(x - 1) * sqrt(x + 1))
    s1 = tl.sqrt(x32 - 1.0)
    s2 = tl.sqrt(x32 + 1.0)
    y32 = tl.log(x32 + s1 * s2)
    y = y32.to(x.dtype)

    tl.store(x_ptr + offsets, y, mask=mask)


def arccosh_(*args, **kwargs):
    logger.debug("GEMS ARCCOSH_")
    if len(args) == 0:
        raise TypeError("arccosh_ expected at least 1 argument (a Tensor)")
    x = args[0]
    if not isinstance(x, torch.Tensor):
        raise TypeError("arccosh_ expected a torch.Tensor as the first argument")

    if not x.is_contiguous():
        raise ValueError("Input tensor must be contiguous")
    if not x.is_floating_point():
        raise TypeError("arccosh_ only supports floating point tensors")

    n_elements = x.numel()
    grid = lambda meta: (triton.cdiv(n_elements, meta["BLOCK_SIZE"]),)
    with torch_device_fn.device(x.device):
        arccosh_kernel_[grid](x, n_elements, BLOCK_SIZE=1024)
    return x
