# Copyright 2026, The FlagOS Contributors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
import logging

import torch
import triton
import triton.language as tl

from flag_gems.utils import pointwise_dynamic

logger = logging.getLogger(__name__)


@pointwise_dynamic(is_tensor=[True, True], promotion_methods=[(0, 1, "DEFAULT")])
@triton.jit
def shifted_chebyshev_polynomial_t_func(x, n):
    # Compute the shifted Chebyshev polynomial of the first kind T_n^*(x).
    # Shifted Chebyshev polynomial of the first kind on [0,1]:
    # T_0^*(x) = 1
    # T_1^*(x) = 2x - 1
    # T_n^*(x) = (4x-2) * T_{n-1}^* - T_{n-2}^*

    xf = x.to(tl.float32)
    nf = n.to(tl.int32)

    # Compute 4x - 2 (the coefficient in recurrence)
    four_x_minus_2 = 4.0 * xf - 2.0

    # T_0^*(x) = 1
    t0 = 1.0
    # T_1^*(x) = 2x - 1
    t1 = 2.0 * xf - 1.0
    # T_2^*(x) = (4x-2) * T_1^* - T_0^*
    t2 = four_x_minus_2 * t1 - t0
    # T_3^*(x)
    t3 = four_x_minus_2 * t2 - t1
    # T_4^*(x)
    t4 = four_x_minus_2 * t3 - t2
    # T_5^*(x)
    t5 = four_x_minus_2 * t4 - t3
    # T_6^*(x)
    t6 = four_x_minus_2 * t5 - t4
    # T_7^*(x)
    t7 = four_x_minus_2 * t6 - t5
    # T_8^*(x)
    t8 = four_x_minus_2 * t7 - t6
    # T_9^*(x)
    t9 = four_x_minus_2 * t8 - t7
    # T_10^*(x)
    t10 = four_x_minus_2 * t9 - t8
    # T_11^*(x)
    t11 = four_x_minus_2 * t10 - t9
    # T_12^*(x)
    t12 = four_x_minus_2 * t11 - t10
    # T_13^*(x)
    t13 = four_x_minus_2 * t12 - t11
    # T_14^*(x)
    t14 = four_x_minus_2 * t13 - t12
    # T_15^*(x)
    t15 = four_x_minus_2 * t14 - t13

    # Select the correct result based on n using arithmetic masking.
    # Arithmetic selection (multiply by bool mask) avoids the tl.where chain
    # issue where out-of-range n values receive indeterminate results.
    result = (
        (nf == 0).to(tl.float32) * t0
        + (nf == 1).to(tl.float32) * t1
        + (nf == 2).to(tl.float32) * t2
        + (nf == 3).to(tl.float32) * t3
        + (nf == 4).to(tl.float32) * t4
        + (nf == 5).to(tl.float32) * t5
        + (nf == 6).to(tl.float32) * t6
        + (nf == 7).to(tl.float32) * t7
        + (nf == 8).to(tl.float32) * t8
        + (nf == 9).to(tl.float32) * t9
        + (nf == 10).to(tl.float32) * t10
        + (nf == 11).to(tl.float32) * t11
        + (nf == 12).to(tl.float32) * t12
        + (nf == 13).to(tl.float32) * t13
        + (nf == 14).to(tl.float32) * t14
        + (nf == 15).to(tl.float32) * t15
    )

    return result.to(x.dtype)


@pointwise_dynamic(is_tensor=[True, False], promotion_methods=[(0, 1, "DEFAULT")])
@triton.jit
def shifted_chebyshev_polynomial_t_func_scalar_n(x, n):
    # Scalar n version for efficiency
    xf = x.to(tl.float32)
    nf = tl.cast(n, tl.int32)

    four_x_minus_2 = 4.0 * xf - 2.0

    t0 = 1.0
    t1 = 2.0 * xf - 1.0
    t2 = four_x_minus_2 * t1 - t0
    t3 = four_x_minus_2 * t2 - t1
    t4 = four_x_minus_2 * t3 - t2
    t5 = four_x_minus_2 * t4 - t3
    t6 = four_x_minus_2 * t5 - t4
    t7 = four_x_minus_2 * t6 - t5
    t8 = four_x_minus_2 * t7 - t6
    t9 = four_x_minus_2 * t8 - t7
    t10 = four_x_minus_2 * t9 - t8
    t11 = four_x_minus_2 * t10 - t9
    t12 = four_x_minus_2 * t11 - t10
    t13 = four_x_minus_2 * t12 - t11
    t14 = four_x_minus_2 * t13 - t12
    t15 = four_x_minus_2 * t14 - t13

    # Select the correct result based on n using arithmetic masking.
    result = (
        (nf == 0).to(tl.float32) * t0
        + (nf == 1).to(tl.float32) * t1
        + (nf == 2).to(tl.float32) * t2
        + (nf == 3).to(tl.float32) * t3
        + (nf == 4).to(tl.float32) * t4
        + (nf == 5).to(tl.float32) * t5
        + (nf == 6).to(tl.float32) * t6
        + (nf == 7).to(tl.float32) * t7
        + (nf == 8).to(tl.float32) * t8
        + (nf == 9).to(tl.float32) * t9
        + (nf == 10).to(tl.float32) * t10
        + (nf == 11).to(tl.float32) * t11
        + (nf == 12).to(tl.float32) * t12
        + (nf == 13).to(tl.float32) * t13
        + (nf == 14).to(tl.float32) * t14
        + (nf == 15).to(tl.float32) * t15
    )

    return result.to(x.dtype)


def special_shifted_chebyshev_polynomial_t(x, n):
    """
    Compute the shifted Chebyshev polynomial of the first kind T_n^*(x).

    The kernel uses pre-computed recurrence values for degrees n in [0, 15].
    For n outside this range, the result is 0.0.

    Args:
        x: Input tensor (float32 only)
        n: Degree of the polynomial (int or int tensor). Supported range: [0, 15].

    Returns:
        Tensor with the same shape as x broadcasted with n
    """
    logger.debug("GEMS SPECIAL_SHIFTED_CHEBYSHEV_POLYNOMIAL_T")
    assert x.dtype in (
        torch.float32,
    ), "Only float32 is supported for special.shifted_chebyshev_polynomial_t"

    if isinstance(n, int):
        return shifted_chebyshev_polynomial_t_func_scalar_n(x, n)
    elif isinstance(n, torch.Tensor) and n.numel() == 1:
        n_val = n.item()
        if isinstance(n_val, int):
            return shifted_chebyshev_polynomial_t_func_scalar_n(x, n_val)
    return shifted_chebyshev_polynomial_t_func(x, n)


__all__ = ["special_shifted_chebyshev_polynomial_t"]
