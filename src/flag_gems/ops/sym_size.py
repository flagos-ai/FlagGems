# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
import logging

import torch

logger = logging.getLogger(__name__)


# sym_size returns the shape of a tensor as a list of integers
# Since this is a metadata operation, we don't need a Triton kernel
# The shape is metadata that can be retrieved directly from the tensor
def sym_size(A: torch.Tensor):
    """Return the size of a tensor as a list of integers.

    Args:
        A: Input tensor

    Returns:
        list of integers representing the tensor shape
    """
    logger.debug("GEMS SYM_SIZE")
    return list(A.shape)
