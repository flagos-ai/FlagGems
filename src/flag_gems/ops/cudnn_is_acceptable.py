# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
import logging

import torch

logger = logging.getLogger(__name__)


def cudnn_is_acceptable(self: torch.Tensor) -> bool:
    r"""Check if the tensor is acceptable for cuDNN operations.

    Returns True when all of the following conditions are met:
    - The tensor is on a CUDA device
    - cuDNN is enabled (torch.backends.cudnn.enabled)
    - The tensor dtype is one of: float16, float32, float64, bfloat16
    - cuDNN is available (torch.backends.cudnn.is_available())

    Args:
        self (torch.Tensor): Input tensor to check.

    Returns:
        bool: Whether the tensor is acceptable for cuDNN.
    """
    logger.debug("GEMS cudnn_is_acceptable")

    if not self.is_cuda:
        return False

    if not torch.backends.cudnn.enabled:
        return False

    if not torch.backends.cudnn.is_available():
        return False

    if self.dtype not in (
        torch.float16,
        torch.float32,
        torch.float64,
        torch.bfloat16,
    ):
        return False

    return True
