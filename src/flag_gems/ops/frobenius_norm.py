# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
# Copyright 2026 FlagOS Contributors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

import logging

import torch
import triton
import triton.language as tl

from flag_gems import runtime
from flag_gems.runtime import torch_device_fn
from flag_gems.utils import dim_compress, libentry
from flag_gems.utils import triton_lang_extension as ext

logger = logging.getLogger(__name__)


@libentry()
@triton.autotune(configs=runtime.get_tuned_config("vector_norm"), key=["M", "N"])
@triton.jit
def frobenius_norm_kernel(X, Out, M, N, BLOCK_M: tl.constexpr, BLOCK_N: tl.constexpr):
    pid = ext.program_id(0).to(tl.int64) * BLOCK_M + tl.arange(0, BLOCK_M)[:, None]
    X = X + pid * N
    Out = Out + pid
    row_mask = pid < M

    inp_dtype = X.type.element_ty
    if inp_dtype == tl.float64:
        acc_dtype = tl.float64
    else:
        acc_dtype = tl.float32
    _sum = tl.zeros([BLOCK_M, BLOCK_N], dtype=acc_dtype)
    for off in range(0, N, BLOCK_N):
        cols = off + tl.arange(0, BLOCK_N)[None, :]
        col_mask = cols < N
        mask = row_mask & col_mask
        a = tl.load(X + cols, mask, other=0.0).to(acc_dtype)
        _sum += a * a
    sum = tl.sum(_sum, axis=1)
    out = tl.sqrt(sum)[:, None]
    tl.store(Out, out, row_mask)


def frobenius_norm(x, dim, keepdim=False):
    logger.debug("GEMS FROBENIUS_NORM")
    dtype = x.dtype
    assert dtype in (
        torch.float16,
        torch.float32,
        torch.bfloat16,
        torch.float64,
    ), f"frobenius_norm not implemented for {dtype}"

    shape = list(x.shape)
    dim = [d % x.ndim for d in dim]
    x = dim_compress(x, dim)
    N = 1
    for i in dim:
        N *= shape[i]
        shape[i] = 1
    M = x.numel() // N

    with torch_device_fn.device(x.device):
        out = torch.empty(shape, dtype=dtype, device=x.device)
        grid = lambda META: (triton.cdiv(M, META["BLOCK_M"]),)
        frobenius_norm_kernel[grid](x, out, M, N)

    if not keepdim:
        out = out.squeeze(dim=dim)
    return out
