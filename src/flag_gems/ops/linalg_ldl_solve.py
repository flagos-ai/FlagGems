# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
import logging

import torch

logger = logging.getLogger(__name__)

_FALLBACK_KEYSET = torch.DispatchKeySet(torch.DispatchKey.CompositeExplicitAutograd)


def linalg_ldl_solve(LD, pivots, B, *, hermitian=False):
    """
    Solve the linear system A @ X = B using LDL factorization.

    This is a wrapper around torch.linalg.ldl_solve that integrates
    with the FlagGems dispatch mechanism.

    Args:
        LD: LDL factorization result from torch.linalg.ldl_factor_ex
        pivots: Pivot indices from torch.linalg.ldl_factor_ex
        B: Right-hand side tensor of shape (*, n, k)
        hermitian: Whether to treat the matrix as Hermitian (for complex types)

    Returns:
        X: Solution tensor of shape (*, n, k)
    """
    logger.debug("GEMS LINALG_LDL_SOLVE")
    if LD.dtype not in (torch.float32, torch.float64) or B.dtype not in (
        torch.float32,
        torch.float64,
    ):
        raise TypeError("linalg_ldl_solve supports only float32 and float64 inputs")
    # Use redispatch to bypass the registered GEMS implementation
    return torch.ops.aten.linalg_ldl_solve.default.redispatch(
        _FALLBACK_KEYSET, LD=LD, pivots=pivots, B=B, hermitian=hermitian
    )
